# Next 공개 앱 환경 변수 (선택)
# 로컬에서는 API가 localhost:3001 이면 기본값으로 동작합니다.
# NEXT_PUBLIC_API_URL=https://your-api.example.com

# 백엔드가 글 변경 시 호출하는 캐시 무효화 API용 시크릿 (백엔드 WEB_REVALIDATE_SECRET과 동일하게 설정)
# REVALIDATE_SECRET=your-shared-secret
